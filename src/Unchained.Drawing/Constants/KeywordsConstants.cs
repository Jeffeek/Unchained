namespace Unchained.Drawing.Constants;

internal static class KeywordsConstants
{
    /// <summary>PDF keyword for cross-reference table start (used in byte scanning).</summary>
    internal const string KeywordXref = "xref";
}
