@inject IJSRuntime JS

<div class="drop-zone @(_isDragging ? "drop-zone--dragging" : "")"
     @ondragenter="OnDragEnter"
     @ondragleave="OnDragLeave"
     @ondragover:preventDefault="true"
     @ondrop="OnDrop"
     @ondrop:preventDefault="true">

    <MudText Typo="Typo.subtitle2" Class="drop-zone-title">@Title</MudText>
    <MudText Typo="Typo.caption" Class="drop-zone-subtitle">@Subtitle</MudText>

    <MudText Class="drop-zone-action" @onclick="OpenFilePicker">
        [ browse… ]
    </MudText>

    <InputFile id="@_inputId"
               OnChange="OnFileSelected"
               accept="@Accept"
               style="display:none;"/>
</div>

@code {

    [Parameter]
    public string Title { get; set; } = "Drop a file here";

    [Parameter]
    public string Subtitle { get; set; } = "or click Browse";

    [Parameter]
    public string Accept { get; set; } = ".pdf";

    [Parameter]
    public long MaxSizeBytes { get; set; } = StudioConstants.MaxUploadBytes;

    [Parameter]
    public EventCallback<(byte[] Bytes, string FileName)> OnFileLoaded { get; set; }

    [Parameter]
    public EventCallback<string> OnError { get; set; }

    private readonly string _inputId = $"file-input-{Guid.NewGuid():N}";
    private bool _isDragging;
    private bool _loading;

    private void OnDragEnter() => _isDragging = true;
    private void OnDragLeave() => _isDragging = false;

    private async Task OnDrop(DragEventArgs e)
    {
        _isDragging = false;
        // JS interop needed for DataTransfer files — trigger the file input instead
        await JS.InvokeVoidAsync("unchainedStudio.clickElement", _inputId);
    }

    private async Task OpenFilePicker() =>
        await JS.InvokeVoidAsync("unchainedStudio.clickElement", _inputId);

    private async Task OnFileSelected(InputFileChangeEventArgs e)
    {
        if (_loading)
            return;

        _loading = true;
        try
        {
            var file = e.File;
            if (file.Size > MaxSizeBytes)
            {
                await OnError.InvokeAsync($"File is too large ({file.Size / 1024 / 1024} MB). Maximum is {MaxSizeBytes / 1024 / 1024} MB.");
                return;
            }

            using var ms = new MemoryStream();
            await using var stream = file.OpenReadStream(MaxSizeBytes);
            await stream.CopyToAsync(ms);
            await OnFileLoaded.InvokeAsync((ms.ToArray(), file.Name));
        }
        catch (Exception ex)
        {
            await OnError.InvokeAsync($"Failed to read file: {ex.Message}");
        }
        finally
        {
            _loading = false;
        }
    }

}
